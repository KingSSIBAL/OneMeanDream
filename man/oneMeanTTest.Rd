% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oneMeanTTest.R
\name{oneMeanTTest}
\alias{oneMeanTTest}
\title{One-Sample t-Test with Wallace Critical Values and Hastings P-Values}
\usage{
oneMeanTTest(x, mu, ha, data, alpha = 0.05, plot = TRUE)
}
\arguments{
\item{x}{Character string. Name of the numeric column in \code{data} to test.}

\item{mu}{Numeric. Hypothesized population mean under the null hypothesis (H0: mu = mu0).}

\item{ha}{Character string. Alternative hypothesis. One of "less" (H1: mu < mu0),
"greater" (H1: mu > mu0), or "not equal" (H1: mu != mu0).}

\item{data}{Data frame containing the column specified in \code{x}.}

\item{alpha}{Numeric. Significance level (0 < alpha < 1). Default is 0.05.}

\item{plot}{Logical. If TRUE (default), display diagnostic plots: Q-Q plot, histogram
with normal curve, and boxplot.}
}
\value{
List of class "oneMeanTTest" containing these fields:
\itemize{
\item variable: Tested variable name
\item n: Sample size (after removing NAs)
\item sample_mean: Sample mean
\item sample_sd: Sample standard deviation
\item t_statistic: t-test statistic
\item p_value: Hastings p-value
\item confidence_interval: Confidence bounds
\item decision: "REJECT" or "FAIL TO REJECT"
\item is_normal: Normality assumption met?
\item outliers: Detected outliers
}
}
\description{
Performs a comprehensive one-sample t-test using Wallace approximation for critical values
and 4-term Hastings approximation for p-values. Includes normality testing, outlier detection,
and diagnostic plots (histogram, Q-Q plot, boxplot).
}
\details{
Critical values use Wallace approximation: \deqn{t_\alpha = \sqrt{\nu (\exp(c \cdot z_\alpha^2 / \nu) - 1)}}
where \eqn{c = ((8\nu+3)/(8\nu+1))^2} and \eqn{\nu = n-1}.

P-values use 4-term Hastings approximation for computational efficiency.
}
\section{Assumptions}{

\enumerate{
\item Normality (Shapiro-Wilk test, p > alpha)
\item No outliers (1.5 * IQR rule)
}
}

\examples{
# Sample data
set.seed(123)
sample_data <- data.frame(
  Earnings = rnorm(30, mean = 71, sd = 2.5)
)

# Left-tailed test
result1 <- oneMeanTTest("Earnings", mu = 75, ha = "less", data = sample_data)
print(result1)

# Right-tailed test (no plot)
result2 <- oneMeanTTest("Earnings", mu = 75, ha = "greater", 
                        data = sample_data, plot = FALSE)

}
